import Button from "./Button"
function Home () {
    return (
        <>
      < Button name="Register" / >
        < Button name="Login" / >
        < Button name="Logout" / > 
        </>
    )
}
export default Home